// Inicia 'i' em 100
let i = 100;
// Enquanto 'i' for maior ou igual a 0
while(i >= 0) {
    // Verifica se 'i' é múltiplo de 3
    if(i % 3 === 0) {
        // Imprime o número
        console.log(i);
    }
    // Diminui 'i' em 1 a cada iteração
    i--;
}
export{};
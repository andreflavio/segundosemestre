/*
E aqui está a explicação condensada para o exercício P.1.2:

- Quando você passa o cursor sobre as variáveis, o TypeScript mostra o tipo delas.
- Para `num` e `str`, os tipos são `number` e `string`, respectivamente, pois foram explicitamente anotados.
- Para `bool`, o tipo é `boolean`, inferido pelo TypeScript com base no valor inicial.
- Para `obj`, o tipo é `{ num: number; str: string; }`, inferido pelo TypeScript com base na estrutura do objeto e nos valores iniciais.
- Para `arr`, o tipo é `number[]`, pois foi explicitamente anotado.



*/
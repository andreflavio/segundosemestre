
// P.1.1. Crie um programa em typescript e defina:

// a) Uma variável que tenha anotação explícita de Cpo numérico.
let num: number = 10;
console.log(num);  // Imprime o valor de 'num'

// b) Uma variável que tenha anotação explícita de Cpo string.Autor: Fabrício Galende Marques de Carvalho, DSc. Página. 13
let str: string = "Hello, world!";
console.log(str);  // Imprime o valor de 'str'

// c) Uma variável que não tenha anotação explícita de Cpo mas que receba um valor lógico.
let bool = true;
console.log(bool);  // Imprime o valor de 'bool'

// d) Uma variável que seja um JSON contendo propriedades de Cpo numérico e de Cpo string.
let obj = { num: 10, str: "Hello, world!" };
console.log(obj);  // Imprime o valor de 'obj'

// e) Um array numérico com anotação explícita de Cpo.
let arr: number[] = [1, 2, 3, 4, 5];
console.log(arr);  // Imprime o valor de 'arr'

